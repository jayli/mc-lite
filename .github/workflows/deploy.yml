# Deploy - 部署工作流（占位）
# 纯前端项目，无需服务器部署
# 如需部署到 CDN 或静态托管，请在此配置

name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Placeholder
        run: echo "No deployment needed for pure frontend project"
